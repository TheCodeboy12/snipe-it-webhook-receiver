# Required: secret key for webhook authentication.
# Requests must include this value as the query parameter: ?key=<WEBHOOK_KEY>
WEBHOOK_KEY=your-secret-key-here

# Optional: port to listen on (default: 8080)
# PORT=8080

# Optional: URL to forward update webhook payloads to.
# Only payloads that match the "update" schema (asset check-in/check-out, etc.) are forwarded.
# Omit or leave empty to disable forwarding.
# FORWARD_WEBHOOK_URL=https://your-slack-or-other-webhook.example.com/hooks/xxx
